<!-- これを partial である members/_form.html.erb の先頭に入れる -->

<% if obj.errors.present? %>
	<div id="errors">
		<h3>エラーがあります。</h3>
		<ul>
			<% obj.errors.full_messages.each do |msg| %>
			<li> <%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>